import { EditorialPage } from "@/components/editorial-page"; import { CopyButton } from "@/components/copy-button";
export const RESEARCH_PROMPT="Audit every active Future Progress Bar value using the current public FPB update manifest. Check each canonical question against up-to-date evidence and return only proposed observation/evidence changes in the FPB update-proposal format. Do not change definitions or thresholds unless you identify a methodology conflict. Do not infer ZERO from absence. Preserve UNKNOWN, LOWER BOUND, and ESTIMATE semantics.";
const CODEX_PROMPT="Apply this approved FPB observation update proposal to the canonical dataset. Preserve FPB-MS semantics, append observation history, advance snapshot metadata, update evidence, run validation/tests/build, and report score impact.";
const SCHEMA=`{\n  "schemaVersion": "FPB-update-proposal-1.0",\n  "snapshotId": "manifest snapshot being audited",\n  "proposals": [{\n    "measurementId": "stable ID",\n    "oldValue": null, "oldStatus": "verified",\n    "proposedValue": null, "proposedStatus": "unknown",\n    "changed": false, "confidence": "high|medium|low",\n    "reason": "...", "sources": [], "scoreImpact": 0\n  }]\n}`;
export default function UpdateGuide(){return <EditorialPage eyebrow="Maintainer operations" title="Update workflow" intro="A controlled five-stage path from fresh research to a validated public snapshot."><section><div className="workflow" aria-label="Update workflow">{["Research","Review","Apply","Validate","Deploy"].map((x,i)=><div key={x}><span>{i+1}</span><strong>{x}</strong></div>)}</div><a className="manifest-card" href="/api/update-manifest"><span>01 · Required input</span><strong>Download current update manifest</strong><small>Machine-readable canonical questions, cutoffs, protocol, and evidence links →</small></a></section><section><p className="eyebrow">Research</p><h2>Audit prompt</h2><p>Use this prompt with the current manifest. The output is a proposal, never a direct data edit.</p><div className="code-panel"><CopyButton text={RESEARCH_PROMPT} label="Copy audit prompt"/><pre><code>{RESEARCH_PROMPT}</code></pre></div></section><section><p className="eyebrow">Review</p><h2>Proposal schema</h2><p>Review confidence, source fit, semantic status, and score impact before application.</p><div className="code-panel"><CopyButton text={SCHEMA} label="Copy schema example"/><pre><code>{SCHEMA}</code></pre></div></section><section><p className="eyebrow">Apply → Validate → Deploy</p><h2>Codex implementation prompt</h2><div className="code-panel"><CopyButton text={CODEX_PROMPT} label="Copy apply prompt"/><pre><code>{CODEX_PROMPT}</code></pre></div><div className="warning-banner"><strong>Validation boundary</strong><p>Codex must validate IDs, statuses, evidence IDs, append-only history, snapshot advancement, and derived scoring. Arbitrary JSON is never blindly ingested.</p></div></section></EditorialPage>}

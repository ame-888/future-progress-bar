import {EditorialPage} from "@/components/editorial-page"; import {SourceExplorer,type SourceExplorerRow} from "@/components/source-explorer"; import {StatCard} from "@/components/ui"; import {ACTIVE_MEASUREMENTS,auditSummary} from "@/lib/transparency"; import {RESEARCH_AUDITS} from "@/lib/research-audits";
export default function SourcesPage(){const audit=auditSummary();const map=new Map<string,SourceExplorerRow>();for(const {measurement:m,domain,field} of ACTIVE_MEASUREMENTS)for(const e of m.evidence){const row=map.get(e.id)??{id:e.id,title:e.title,organization:e.organization??"Organization not recorded",url:e.url,sourceType:e.sourceType,publicationDate:e.publicationDate??"",accessedDate:e.accessedDate,claims:[]};row.claims.push({measurementId:m.id,measurementTitle:m.title,domain,field,supports:e.supports});map.set(e.id,row)}const outstanding=audit.evidenceCoverage.total-audit.supportCoverage.complete;return <EditorialPage eyebrow="Canonical support registry" title="Evidence explorer" intro="External sources are normalized by stable ID and linked to their exact claims. Public research audits are reproducibility records—not external sources."><section className="stat-grid"><StatCard label="Unique external sources" value={audit.evidenceCoverage.uniqueSources}/><StatCard label="Measurements with evidence" value={`${audit.evidenceCoverage.withEvidence}/${audit.evidenceCoverage.total}`}/><StatCard label="Research audits" value={`${RESEARCH_AUDITS.length}/${audit.evidenceCoverage.total}`}/><StatCard label="Complete support" value={`${audit.supportCoverage.complete}/${audit.supportCoverage.total}`}/><StatCard label="Outstanding warnings" value={outstanding}/></section><SourceExplorer rows={[...map.values()]}/><section><p className="eyebrow">RESEARCH AUDIT</p><h2>Public reproducibility records</h2><p>These records document search scope, candidate screens, conclusions, and limitations. They are deliberately not counted as external sources.</p>{RESEARCH_AUDITS.map(a=><article key={a.id}><h3>{a.measurementId} · {a.completeness}</h3><p>{a.conclusion}</p><p><strong>Limitations:</strong> {a.limitations}</p></article>)}</section></EditorialPage>}

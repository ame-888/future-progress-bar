import { PageHeader } from "@/components/ui"; import { ResearchRegistry, type RegistryRow } from "@/components/research-registry"; import { ACTIVE_MEASUREMENTS, compactRecord } from "@/lib/transparency";
export default function ResearchPage(){const rows:RegistryRow[]=ACTIVE_MEASUREMENTS.map(x=>{const c=compactRecord(x.measurement);return{id:c.id,title:c.title,question:c.question,domain:c.domain,field:c.field,answer:String(c.displayValue??c.currentValue??c.status),status:c.status,freshness:c.freshness,evidenceCount:c.evidenceCount,definitionVersion:c.definitionVersion,fullSpecUrl:c.fullSpecUrl}});return <main className="wide-page"><PageHeader eyebrow="Canonical registry · 48 records" title="Research atlas" intro="Search every measurement, inspect its epistemic state, and open the complete versioned specification behind each public result." meta={<span>FPB-MS canonical data · observed results only</span>}/><ResearchRegistry rows={rows}/></main>}

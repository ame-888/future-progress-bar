import Link from "next/link"; import { EditorialPage } from "@/components/editorial-page"; import { changelog } from "@/lib/transparency";
export default function ChangelogPage(){const log=changelog();return <EditorialPage eyebrow="Canonical history" title="Changelog" intro="Reality/data changes and FPB methodology changes are deliberately separate."><section><h2>Observation changes</h2><p>The world or source data changed.</p>{log.observations.map((x,i)=><article key={`${x.measurementId}-${i}`}><h3><Link href={`/research/${x.measurementId}`}>{x.title}</Link></h3><p>{x.researchCutoff} · {x.valueStatus} · after: {x.value??"nonnumeric"}</p><p>{x.reasonForRevision??x.note??x.details?.join(" ")??"Observation recorded."}</p></article>)}</section><section className="definition-history"><h2>Definition changes</h2><p>FPB changed its methodology, not the world.</p>{log.definitions.map(x=><article key={`${x.measurementId}-${x.definitionVersion}`}><h3><Link href={`/research/${x.measurementId}`}>{x.title}</Link></h3><p>{x.effectiveFrom} · definition v{x.definitionVersion}</p><p>{x.reason} {x.migrationNote}</p><p>Changed: {x.changedFields.join(", ")}{x.comparabilityBreak?" · comparability break":""}</p></article>)}</section></EditorialPage>}

import { inspectCatalogue } from "../src/lib/catalog-validation.ts"; import { ACTIVE_MEASUREMENTS, auditSummary, compactRecord, qualitySignals } from "../src/lib/transparency.ts";
const command=process.argv[2]??"summary", inspection=inspectCatalogue(), audit=auditSummary();
if(command==="validate"){console.log(`FPB canonical validation: ${inspection.errors.length} error(s), ${inspection.warnings.length} warning(s)`); inspection.warnings.forEach(x=>console.log(`WARNING ${x}`)); if(inspection.errors.length){inspection.errors.forEach(x=>console.error(`ERROR ${x}`));process.exitCode=1;}}
else if(command==="audit"){console.log(JSON.stringify({summary:audit,quality:ACTIVE_MEASUREMENTS.map(({measurement})=>({id:measurement.id,signals:qualitySignals(measurement)}))},null,2));}
else {console.log(`Snapshot ${audit.snapshotId} · FPB-MS ${audit.standardVersion}`); console.log(`Score ${audit.score.achieved}/${audit.score.possible} · ${audit.measurements} measurements · evidence ${audit.evidenceCoverage.withEvidence}/${audit.evidenceCoverage.total} · stale ${audit.stale}`); ACTIVE_MEASUREMENTS.forEach(({measurement})=>{const x=compactRecord(measurement);console.log(`${x.id}\t${x.status}\t${x.displayValue??x.currentValue??"—"}\t${x.freshness}\t${x.question}`)});}
